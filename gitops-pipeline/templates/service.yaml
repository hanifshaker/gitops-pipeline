apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-pipeline.fullname" . }}
  labels:
    {{- include "gitops-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-pipeline.selectorLabels" . | nindent 4 }}
